# 系统架构与代码审查报告 (System Evaluation)

通过对您项目的各个组件（前端 `Vue 3`、后端 `FastAPI` + `SQLite/APScheduler`、`Dockerfile` 环境定义等）进行全方位的审视分析，我发现虽然当前架构能够很好地满足轻量化的任务调度与执行需求，但在**系统健壮性、资源管理及细节优化**上，仍存在一些在长期使用时会暴露出来的潜在隐患。

以下是需要或建议调整的核心问题清单，按优先级从高到低排序：

## 🔴 高优先级：可能导致宕机或严重的资源泄漏

### 1. 进程泄漏 (Zombie Processes) 与僵尸清理
*   **现象/原因**：在 `backend/app/main.py` 的 `run_script_task` 方法中，当脚本执行超时时，您使用了 `proc.kill()` 来结束任务。但 `proc.kill()` 只能杀掉紧跟着的子进程（比如 `xvfb-run` 或者 `python` 解释器）。该进程由于调用了 Playwright 或者 Selenium，其派生出的后台浏览器进程（如 `chrome` 或 `node` 服务）并不会被杀死，从而沦为了**“孤儿/僵尸进程”**。
*   **后果**：随着时间推移，如果经常有超时失败的任务，服务器的运行内存 (RAM) 最终会被无止境的死浏览器进程吃光，导致整个容器 OOM 宕机。
*   **建议调整**：
    *   启动子进程时分配一个新的进程组标识（`start_new_session=True` 或使用 `os.setpgrp`）。
    *   在超时触发时，使用 `os.killpg(os.getpgid(proc.pid), signal.SIGTERM)` 一次性剿灭整个进程组（连带所有浏览器实例）。
    *   **Dockerfile 建议添加 `tini` 或 `dumb-init`**：既然容器需要频繁衍生子进程，入口命令直接使用 `uvicorn` 作为 PID 1 是非常危险的，推荐使用 `ENTRYPOINT ["/usr/bin/tini", "--"]` 来接管僵尸进程回收。

### 2. 同步阻塞引发的 FastAPI 异步循环假死
*   **现象/原因**：FastAPI 的事件循环运行在单个主线程中。在代码的 `prepare_env` (如创建虚拟环境) 中，您直接使用了**阻塞式**的系统调用：`subprocess.run([sys.executable, "-m", "venv", env_dir], check=True)` 和 `npm init`。
*   **后果**：创建一个 Python venv 甚至在磁盘 I/O 较慢的机器上需要 2-5 秒以上的时间。在这段时间里，整个后端的异步事件大循环会**完全冻结**。任何这期间尝试访问网页或 API 的用户都会感觉到应用无响应。
*   **建议调整**：所有系统命令均应替换为 `asyncio.create_subprocess_exec(...)` 或将 `subprocess.run` 丢入 `asyncio.to_thread()` 去执行。

### 3. 重启状态一致性 (永久锁定问题)
*   **现象/原因**：如果容器因为宿主机断电、更新镜像或 OOM 崩溃而死，此时恰逢某个任务在执行，其数据库中状态记录的是 `Running`。
*   **后果**：当容器重新启动后（`startup_event`），该任务会**永远卡在 `Running` 状态**，并且时间停止，直到您手动进入系统去点击强制停止。
*   **建议调整**：由于系统是单节点的，在 `startup_event` 时，应该编写一条 SQL 或者 ORM 操作，将所有状态依然为 `"Running"` 的任务统一置为 `"Cancelled" 或者 "Failed (Killed)"`。

---

## 🟡 中优先级：长期维护与安全隐患

### 4. JWT 会话的易失性 (登录态频繁掉线)
*   **现象/原因**：`SECRET_KEY = os.getenv("JWT_SECRET", secrets.token_hex(32))`。
*   **后果**：如果用户或者 `docker-compose.yml` 没有显式配置 `JWT_SECRET`，每次 Docker 重启后端服务时，它都会自动生成一个新的32位随机密钥。这会导致旧的密钥全部失效，**所有正在使用系统的用户（即便勾选了记住密码）都会被强制登出并弹回 Login 页面**。
*   **建议调整**：要么强烈要求在文档/配置中提供一个静态的 Secret 环境变量；要么如果没有提供，让系统将这份生成的密钥持久化保存进 `data/` 目录中个隐藏文件里，而非停留在内存。

### 5. API 日志无限膨胀 (Log Bloating)
*   **现象/原因**：脚本的执行日志是被直接 JSON 序列化为文本存入了 SQLite 的 `Script.last_log` 字段里。
*   **后果**：当您执行一些非常啰嗦的 Node.js 爬虫（比如每秒抛出上百次 Debug 信息）且没有自行截断时，这个字符串字段会极其庞大。每次进入首页请求 `/api/scripts` 时（您现在请求的是全量字段），这些成吨的无用日志文本都会塞满 HTTP 响应包，让页面卡死。
*   **建议调整**：对接收的子进程日志进行行数或者字符数限制（例如通过队列截取最后 1000 行），或考虑把 `log` 抽离到一张单独的表 / 磁盘日志文件中。

### 6. 前端路由缺乏安全守卫 (Route Guard)
*   **现象/原因**：当前虽然写了 Axios 的拦截器在 401 时跳转至 `/login`。但在页面级别的初始加载是没有验证拦截的。
*   **后果**：即使用户没有权限或者 token 已经过期，打开应用的第一时间，仍然会先加载并渲染被禁止的布局组件和 Dashboard，只是一小会之后发现接口报 401 才会将其踢出，页面会出现明显的“闪烁（即由登录后界面跳回登录前界面）”，体验不佳。
*   **建议调整**：在 `frontend/src/router/index.js` 添加 `Vue Router` 的 `beforeEach` 守卫，判断 `localStorage.getItem('token')` 是否存在后再准许放行。

---

## 🟢 低优先级：体验提升与优化

### 7. 频繁无意义的环境重复安装
*   每次执行时都进行 `pip install -r requirements.txt` 或者 `npm install`。虽然 `npm` / `pip` 底层有自身缓存，但这仍然会增加几十秒无意义的通信开销。
*   **优化想法**：可以在每次更新对应环境配置依赖保存后，对其哈希值（MD5/SHA1）进行记录，只有当运行前发现依赖项文本发生了变更时，才触发新一轮的包管理安装。

### 8. Playwright 与 Selenium 并存的冗余性
*   您的基础镜像 `Dockerfile` 已经安装且内置了 Playwright Chromium 以及系统级的 Chrome/Chromium。但是在 `requirements` 或脚本依然倾向于拉取庞大的 webdriver。虽然这增加了极强灵活性的扩展兼容，但我建议在文档使用说明中，强烈推荐优先使用 Playwright 原生调用内置 Chrome 以获取最佳和最不容易崩溃的交互。

---

**总结**
这套项目其实**非常务实且精妙**，但主要是由于其结合了“Python / NodeJS 子进程调用”和“Docker容器化”两把双刃剑，极易触碰资源泄漏的红线（尤僵尸进程问题较为典型和关键）。

如果您觉得上述报告中的任何项需要立即纠正，您可以指定我立刻投入到相应的调整中。例如：“帮我解决僵尸进程并添加 tini”或“修复 FastAPI 同步假死问题”。我可以马上排期制定实施计划执行。
